source "http://rubygems.org"

gemspec path: File.expand_path('../..', __FILE__)

gem 'rack'
gem 'rake'
gem 'rspec'
gem 'timecop'
gem 'beefcake'

group :ruby_2 do
  gem 'allocation_counter', github: 'wycats/allocation_counter', branch: 'master'
end

unless ENV['SKIP_EXTERNAL']
  gem 'excon'
  gem 'moped'
end
