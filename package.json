{
  "name": "skylight-agent",
  "version": "5.0.1",
  "private": true,
  "scripts": {
    "lint": "npm-run-all --aggregate-output --continue-on-error --parallel 'lint:!(fix)'",
    "lint:prettier": "prettier --loglevel log --check .",
    "lint:prettier:fix": "prettier --write ."
  },
  "devDependencies": {
    "@prettier/plugin-ruby": "^3.2.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.1"
  },
  "volta": {
    "node": "14.16.1",
    "yarn": "1.22.10"
  }
}
